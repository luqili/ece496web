<div class="container">
	<h1>Demo</h1>
</div>
<div class="container">
	<!-- if demos -->
	{{#if demos}}
		{{#each demos}}
		<h2>{{name}}
			<div class="pull-right text-muted">19th May 2014</div>
		</h2>
		<div class="row demo-images">
				{{#if heroImage}}
				<div class="col-sm-4 col-md-4 demo-image">
					<img src="{{cloudinaryUrl heroImage width='680' height='680'}}" class="img-rounded">
				</div>
				{{/if}}
				<div class="col-sm-8 col-md-8">
					<div class="row">
						{{#each images}}
						<div class="col-xs-4 col-sm-4 col-md-3 demo-image">
							<img src="{{cloudinaryUrl width='300' height='300'}}" class="img-rounded">
						</div>
						{{/each}}
					</div>
				</div>
		</div>
		{{/each}}
	{{else}}
	<!-- else -->
	<h3 class="text-muted">The facial expression transferring online is coming soon!</h3>
	{{/if}}
</div>
